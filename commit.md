# Commit Instructions

1. Check the working tree for changes: `git status --short`
2. Stage files to commit: `git add <paths>`
3. Create a commit with a clear message: `git commit -m "Describe the change"`
4. Verify the commit: `git show --stat -1`
